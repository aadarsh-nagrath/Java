Perfect. Interfaces are one of the most important Java concepts for backend development.

Forget syntax for a minute.

Why do interfaces exist?

Suppose you build a payment system.

Today:

Stripe

Tomorrow:

Stripe
Razorpay
PayPal

You don't want the rest of your code to care which payment provider is being used.

So you define a contract.

interface PaymentService {
    void pay();
}

This says:

"Anyone who claims to be a PaymentService must implement pay()."

No implementation yet.

Implementation
class StripePaymentService implements PaymentService {

    @Override
    public void pay() {
        System.out.println("Paying via Stripe");
    }
}

Another implementation:

class RazorpayPaymentService implements PaymentService {

    @Override
    public void pay() {
        System.out.println("Paying via Razorpay");
    }
}

Notice:

extends → inherit from a class.
implements → implement an interface.
Usage
PaymentService payment = new StripePaymentService();

payment.pay();

This should remind you of:

Animal a = new Dog();

Same idea.

Reference type = PaymentService
Object type = StripePaymentService

---------------------------------------------------------------------------

Loose Coupling

This is a very common interview topic.

Tight coupling ❌
StripePaymentService payment = new StripePaymentService();

Your code now depends directly on Stripe.

If tomorrow you want Razorpay, you have to change all places where StripePaymentService is used.

Loose coupling ✅
PaymentService payment = new StripePaymentService();

Your code depends only on the interface.

Tomorrow you can do:

PaymentService payment = new RazorpayPaymentService();

The rest of your code doesn't change because it only knows:

payment.pay();

not how payment is implemented.


---------------------------------------------------------------------------
ok i got it so if interface A has a method anyhting implementing it should also have that method .


but if it were a class that is not mandatory 

✅ Exactly.

Now your second question:

If it were a class, is that mandatory?

No.

Example:

class Animal {
    void eat() {
        System.out.println("Eating");
    }
}

class Dog extends Animal {

}

This compiles.

Why?

Because Dog inherits eat() from Animal.

It doesn't have to write it again.

If Dog wants different behavior, it can override it.

class Dog extends Animal {
    @Override
    void eat() {
        System.out.println("Dog eating");
    }
}
Biggest difference

Class (extends)

Gets implementation from parent.
Can override if needed.

Interface (implements)

Gets only the contract.
Must provide the implementation (unless the class is abstract).
Interview one-liner

A class provides implementation + state, whereas an interface primarily defines a contract that implementing classes must follow.

This distinction is fundamental in Java and comes up frequently in interviews.


---------------------------------------------------------------------------


Interface extending another interface
interface Animal {
    void eat();
}

interface Pet extends Animal {
    void play();
}

Now any class implementing Pet must implement both methods.

class Dog implements Pet {

    @Override
    public void eat() {
        System.out.println("Eating");
    }

    @Override
    public void play() {
        System.out.println("Playing");
    }
}

Think of it like:

Animal
   ↑
 Pet

Dog implements Pet

Since Pet extends Animal, Dog indirectly agrees to both contracts.

The difference is what Animal is:

class Animal → inheritance (extends)
interface Animal → implementation (implements)
Your intuition question

Why not extends an interface?

Because an interface is not a class. It has no object state to inherit from.

Classes extend classes.
Classes implement interfaces.
Interfaces can extend other interfaces.



---------------------------------------------------------------------------

Which of these are valid?

interface A {}
interface B {}

1 class C implements A {}
2 class C implements A, B {}
3 class C extends A {}
4 interface C extends A, B {}

Your quiz

You answered 1, 2.

Correct answer is:

✅ 1, 2, and 4

4 is valid because interfaces can extend multiple interfaces:

interface A {}
interface B {}

interface C extends A, B {}

This is one of Java's design choices:

❌ A class cannot extend multiple classes.
✅ An interface can extend multiple interfaces.
✅ A class can implement multiple interfaces.

-------------------------------------------------------------------
Current level

You've got a solid grasp of:

✅ Classes
✅ Objects
✅ References
✅ Constructors
✅ this
✅ Access modifiers (basics)
✅ Inheritance
✅ Upcasting & Downcasting
✅ Method overriding
✅ Interfaces
✅ extends vs implements

This is roughly the point where many Java learners start feeling comfortable reading real code.

Next roadmap (in order)
Abstract classes ⭐ (20 min)
super keyword
final
Exception Handling (try-catch, throws)
Collections (List, Set, Map) ← heavily used in backend
Generics
Lambda expressions
Streams API
JVM memory (Heap, Stack, GC)
Multithreading
Spring Boot


---------------------------------------------------------------------------

Abstract Class

Think of it as something between a class and an interface.

Example:

abstract class Animal {

    void eat() {
        System.out.println("Eating");
    }

    abstract void speak();
}

Notice two things:

void eat() {}

✅ Has implementation.

abstract void speak();

❌ No implementation.

An abstract class can have both.

Now:

class Dog extends Animal {

    @Override
    void speak() {
        System.out.println("Bark");
    }
}

Dog must implement speak() because it's abstract.

But it inherits eat() automatically.

Can we do this?
Animal a = new Animal();

❌ No.

Why?

Because an abstract class is incomplete.

It's like saying:

"There will be different kinds of Animals, but I don't know how every Animal speaks."

So Java doesn't let you create an object of it.

Mental model
Class	Abstract Class	Interface
Complete implementation	Partial implementation	Contract
Can create object	❌ Cannot create object	❌ Cannot create object
Interview rule (don't memorize, understand)

Use an abstract class when subclasses share common state or behavior.

Example:

abstract class Animal {
    String name;

    void eat() {
        System.out.println("Eating");
    }

    abstract void speak();
}

Every animal has a name and can eat(), but each animal speak()s differently.

An interface is better when you want unrelated classes to follow the same contract.


---------------------------------------------------------------------------
1. If a class has even one abstract method

Example:

abstract class Animal {
    abstract void speak();
}

Then:

Animal a = new Animal();

❌ Not allowed.

The reason is not "because it has one abstract method."

The reason is:

The class itself is declared abstract.

In Java, if a class contains an abstract method, the class must be declared abstract.

2. Is there an abstract variable?

❌ No.

Only these can be abstract:

Classes
Methods

Fields (variables) cannot be abstract.


---------------------------------------------------------------------------
3. Your quiz
abstract class Animal {
    abstract void speak();
}

class Dog extends Animal {

}

❌ It will NOT compile.

Why?

Dog is a concrete class (not abstract), so it must implement all inherited abstract methods.

You have two options:

class Dog extends Animal {
    @Override
    void speak() {
        System.out.println("Bark");
    }
}

or

abstract class Dog extends Animal {

}

because now Dog is also abstract and is allowed to leave speak() unimplemented.

Rule (worth remembering)

If a class extends an abstract class, it must do one of these:

Implement all abstract methods.
Declare itself abstract.


ok now i get it but if it were a public class Animal, with public method speak then it would have compiled . 
but here as Dog is normal class it acts as a implement thing you need that method in the dog class for compilation. or just make dog abstract class


--------------------------------------------------------------------------

Important fact (interview favorite)

Even if you don't write super(), Java inserts it automatically.

So these are equivalent:

Dog() {
    super();
    System.out.println("Dog");
}

and

Dog() {
    System.out.println("Dog");
}

The compiler secretly adds super(); as the first statement.

The only time you write it yourself is when the parent has a constructor with parameters:

class Animal {
    Animal(String name) {}
}

class Dog extends Animal {
    Dog() {
        super("Dog");
    }
}

because there is no no-argument constructor to call.
---------------------------------------------------------------------------

Tiny quiz (last one on super)

What happens here?

class Animal {
    Animal(String name) {}
}

class Dog extends Animal {
    Dog() {

    }
}

Does it compile?

A. Yes

B. No

Why? (One line.)

No it wont as compiler cannot add super() as super class constructor has a parameter name 

🎯 Perfect. That's exactly right.

Interview answer:

No. The compiler inserts super() only if the parent has a no-argument constructor. Since Animal only has Animal(String name), Dog must explicitly call super("...").

Example:

class Animal {
    Animal(String name) {}
}

class Dog extends Animal {
    Dog() {
        super("Dog");
    }
}

---------------------------------------------------------------------------
Great. Next: final.

This keyword has 3 different meanings depending on where it's used.

1. final variable
final int age = 25;
age = 30;   // ❌

Once assigned, it cannot be reassigned.

2. final method
class Animal {
    final void eat() {}
}

class Dog extends Animal {
    void eat() {}   // ❌
}

A final method cannot be overridden.

3. final class
final class Animal {}

class Dog extends Animal {}   // ❌

A final class cannot be extended.

Example: String in Java is final.

⚠️ Interview trap
final Car c = new Car();

Can we do:

c = new Car();

❌ No.

Can we do:

c.brand = "BMW";

✅ Yes.

Why?

Because final makes the reference variable immutable, not the object.

Think:

final c  ─────► Car Object

You cannot make c point somewhere else, but you can still modify the object it points to (unless the object's fields are also immutable).

---------------------------------------------------------------------------

Why is String final?

Not exactly.

String isn't final because it's a data type. It's final because Java wants strings to be immutable and safe.

If inheritance were allowed:

class MyString extends String {
    // imagine changing behavior...
}

someone could change how strings behave, breaking assumptions across the JVM.

Benefits of String being immutable/final include:

Security (file paths, URLs, passwords, class names can't be altered unexpectedly)
Thread safety
String Pool optimization
Reliable hashCode() (important for HashMap)

You don't need to memorize all of these now. Just remember:

String is final so its behavior cannot be changed through inheritance, helping keep strings immutable and safe.


---------------------------------------------------------------------------
nt a = 10;
int b = 0;

System.out.println(a / b);
✅ Correct. C. Runtime exception.

More precisely:

ArithmeticException: / by zero

Java throws an exception at runtime because the operation is illegal.

What is an Exception?

An exception is an object that represents an error occurring during program execution.

Think of it as:

Program running
      │
      ▼
Something unexpected happens
      │
      ▼
Java creates an Exception object
      │
      ▼
Either you handle it or the program terminates


And your explanation is interview-worthy:

finally executes regardless of whether an exception occurs or is caught.

One small caveat: there are rare cases (e.g., System.exit() or JVM crash) where finally won't execute, but ignore those for now.

try {
    System.out.println("A");
} catch (Exception e) {
    System.out.println("B");
} finally {
    System.out.println("C");
}


---------------------------------------------------------------------------
throws

You declare that a method may throw an exception.

void readFile() throws IOException {
    ...
}

Think: "I'm warning the caller that this method might throw an IOException."

The caller must then decide whether to handle it or propagate it.

Easy way to remember
throw → inside a method, throws one exception object.
throws → in the method signature, declares possible exceptions.

First, one correction:

throw new IOException();

❌ This is not a custom exception.

IOException is a built-in Java exception.

A custom exception would be something you create:

class InvalidAgeException extends Exception {
}

Then:

throw new InvalidAgeException();
Why do we need throws?

Suppose you write:

void readFile() throws IOException {
    // code that may fail while reading a file
}

You're telling Java:

"This method might throw an IOException. I'm not handling it here."

Now another method calls it:

void process() {
    readFile();
}

Java says:

❌ "Hold on. readFile() might throw an IOException. What are you going to do if that happens?"

So you have two choices.

Option 1: Handle it
void process() {
    try {
        readFile();
    } catch (IOException e) {
        System.out.println(e.getMessage());
    }
}
Option 2: Pass the responsibility
void process() throws IOException {
    readFile();
}

Now process() is also telling its caller:

"I don't handle it either. You deal with it."

The responsibility keeps moving up until someone catches it.

Should every method have throws?

❌ No.

Only methods that can throw checked exceptions and choose not to handle them.

If your method can handle the exception itself, use try-catch and don't declare throws.

Mental model

Think of throws as forwarding a package.

readFile() ──► process() ──► main()

       IOException

Each method can either:

📦 Open the package (try-catch), or
📦 Pass it to the next person (throws).

---------------------------------------------------------------------------

Checked vs Unchecked Exceptions.

This is asked in almost every Java interview.

1. Checked Exceptions

The compiler forces you to handle them.

Example:

IOException
SQLException

If a method throws one of these:

void readFile() throws IOException

then the caller must either:

use try-catch, or
declare throws IOException.

Otherwise, it won't compile.

2. Unchecked Exceptions

The compiler does not force you to handle them.

Examples:

ArithmeticException
NullPointerException
IndexOutOfBoundsException
IllegalArgumentException

You can catch them, but you don't have to.

Example:

int x = 10 / 0;

This compiles fine, but at runtime:

ArithmeticException
Easy way to remember
Checked	Unchecked
Compiler checks	Compiler doesn't check
Must handle or declare	Optional to handle
Usually external failures (files, DB, network)	Usually programming bugs

---------------------------------------------------------------------------
Compile time vs Runtime
Compile time

This happens when Java converts:

.java  →  .class

using:

javac Main.java

The compiler checks things like:

Syntax errors
Type errors
Missing methods
Access modifier violations
Checked exceptions not handled

Example:

int x = "hello";

❌ Compile error.

Program never runs.

Runtime

This happens when you execute:

java Main

The JVM is running your program.

Errors that happen now:

int x = 10 / 0;

Compiles ✅

Runs ❌

ArithmeticException
Should we handle both?

No. They are different categories.

You don't "handle" compile errors.

You fix them.

Example:

String name = 100;

You don't catch this with try-catch.

You correct the code.

Runtime exceptions can be handled:

try {
    int x = 10 / 0;
}
catch (ArithmeticException e) {
    System.out.println("Invalid division");
}
Interview summary:

Compile-time errors are detected by the compiler before execution. Runtime errors occur while the program is running and may be handled using exception handling.


